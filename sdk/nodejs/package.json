{
    "name": "@pulumiverse/github-credentials",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource github-credentials ${VERSION}"
    },
    "dependencies": {
        "@pulumi/aws": "^4.0.0",
        "@pulumi/google-native": "0.29.0",
        "@pulumi/pulumi": "^3.42.0"
    },
    "devDependencies": {
        "@types/node": "^14",
        "typescript": "5.0.4"
    },
    "pulumi": {
        "resource": true,
        "name": "github-credentials"
    }
}
